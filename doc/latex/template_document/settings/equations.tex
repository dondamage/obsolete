%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%   equations.tex
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

% general formatting
\newcommand{\binmat}[1]{\mathbf{#1}} % formatting for matrices
\newcommand{\binvec}[1]{\textbf{#1}} % formatting for vectors

% frequently used formulas
\newcommand{\dBDec}[0]{\,\frac{\mathrm{dB}}{\mathrm{decade}}} % dB/decade
\newcommand{\diff}[0]{\mathrm{d}} % upstanding d for differential operator
\newcommand{\jcompl}[0]{\mathrm{j}} % upstanding j for imaginary unit
\newcommand{\unit}[1]{\,\mathrm{#1}} % unit upstanding and spacing to number
\newcommand{\ul}[1]{\underline{#1}} % underline

% frequently used units
\newcommand{\dB}[0]{\unit{dB}} % dB, decibel

% frequently used items
\newcommand{\fclk}[0]{f_{\mathrm{clk}}} % f_clk
\newcommand{\Tclk}[0]{T_{\mathrm{clk}}} % T_clk
